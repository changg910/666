# 客戶月銷貨追蹤系統 - 使用說明（v3：新增歷年趨勢比較）

> **這是獨立的新版本**，跟你原本在用的客戶追蹤系統是分開的資料夾。你原本的系統完全沒有被更動，這份新檔案只是「多加了一個歷年趨勢比較頁面」，其餘功能與畫面都跟原本一模一樣。

## 如何安全試用（不影響原系統）

這份新檔案讀寫資料用的 IndexedDB 資料庫名稱、欄位跟你原本系統完全相同（`cst_tracker_db`），所以：

- 如果你是**先關掉原本資料夾的 Live Server，再用同一個 VS Code 視窗開這個新資料夾**，通常會用同一個網址 port（例如 `127.0.0.1:5500`），瀏覽器會認為是同一個網站，**直接讀到你原本真實的客戶與銷貨資料**。
- 如果你是**兩個資料夾同時開著 Live Server**，因為 port 會不一樣，瀏覽器會認為是不同網站，這個新版本一開始會是**空的**（不會動到原本資料，只是這個新分頁暫時看不到舊資料）。這種情況下要看到真實資料，可以用左下角「匯出全部資料備份」把原系統資料匯出，再到這個新版本用「還原備份檔」讀進來測試。

不管哪種情況，**都不會動到你原本系統的任何資料**。確認新版本沒問題、想正式改用它之後，你可以自己決定要不要把這三個檔案覆蓋過原本的資料夾——如果要覆蓋，記得先在原系統匯出一份備份，這樣萬一有任何問題，用「還原備份檔」就能完整復原。

## 新增：歷年趨勢比較

側邊選單「歷年趨勢比較」是這次新增的功能，用法跟產品銷貨追蹤系統裡的同名頁面完全一樣，只是比較對象換成**客戶**：

- **比較單位**：依客戶／依分類（分類使用客戶管理裡的「分類」欄位）
- 左側先選分類，再把客戶拖拉（或點選）到右側「已選擇比較」，最多同時比較8位（依分類時上限依你有幾個分類自動調整）
- 支援「連續區間」與「跨年同期比較」兩種模式，可切換年度／月度顆粒度、金額／數量指標，也可以顯示加總線
- 明細表格會凍結表頭與期間欄，並用顏色標示每位客戶（或分類）的欄位群組
- **已合併的客戶編號會自動加總**：跟 Dashboard、客戶分析頁一樣，比較數字是用「合併計算後」的結果，不會因為同一位客戶有多個編號而被拆開計算

---


> **儲存方式更新**：系統現在改用瀏覽器的 IndexedDB 儲存資料（原本是 localStorage，容量上限只有約 5-10MB，資料多了容易存滿）。IndexedDB 容量上限通常是幾百MB到數GB，一般不會再遇到存滿的問題。第一次用新版打開網站時，系統會**自動偵測並搬移**你原本存在 localStorage 裡的舊資料，不需要手動處理，也不會遺失資料。左下角「檢查儲存空間」可以隨時查看目前用量。

## 一、在 VS Code 開啟網站（第一次設定）

1. 打開 VS Code，選擇「File（檔案）→ Open Folder（開啟資料夾）」，選擇這個 `customer-sales-tracker` 資料夾。
2. 到 VS Code 左側「Extensions（擴充功能）」，搜尋 **Live Server**（作者 Ritwick Dey），點擊 Install 安裝。
3. 安裝完成後，在檔案總管點右鍵點開 `index.html`，選擇 **「Open with Live Server」**。
4. 瀏覽器會自動打開網站，之後每次要用，重複步驟 3 即可。

> 若你電腦裡已經用過舊版網站（沒有「產品管理」的版本），打開新版網站時系統會**自動偵測並升級**舊資料，客戶與銷貨紀錄都會保留，只是產品欄位會是空的。

## 二、重要：資料存在「這台電腦、這個瀏覽器」

- 每次更新完銷貨資料後，點左下角「匯出全部資料備份」，把 `.json` 備份檔存到雲端硬碟。
- 若要換電腦或資料遺失，用左下角「還原備份檔」上傳備份檔即可完整還原。

## 三、六大功能簡介

1. **首頁 Dashboard**：選擇檢視月份，看本月總銷貨、成長/下滑/停購客戶數，以及所有客戶的「客戶脈動」總覽（跨所有產品加總）。
2. **客戶管理**：新增/編輯/刪除客戶（客戶編號、名稱、分類、聯絡人、電話、備註）。
3. **產品管理**：新增/編輯/刪除產品（產品編號、產品名稱）。透過 Excel 匯入時，系統也會自動建立新產品。
4. **每月銷貨資料**：手動新增/編輯/刪除單筆交易紀錄（客戶＋產品＋數量＋單價＋金額），可用年、月、客戶、產品篩選。
5. **Excel 批次匯入**：上傳銷貨交易明細 Excel 檔（欄位：單據日期／客戶編號／客戶簡稱／產品名稱(含編號)／數量／單價／金額），系統自動比對或建立客戶與產品，並偵測重複匯入的交易自動略過。
6. **客戶分析**：選擇單一客戶，看該客戶近 12 個月銷貨趨勢、**各產品的月度成長/下滑/停購狀況**，以及完整歷史交易紀錄。

## 四、Excel 匯入格式說明

匯入的 Excel 檔第一列（標題列）需包含：

| 單據日期 | 客戶編號 | 客戶簡稱 | 產品名稱(含編號) | 數量 | 單價 | 金額 |
|---|---|---|---|---|---|---|
| 2026/07/04 | B009 | 柏強商行 | 防水能 立裝 草綠色(A1001-1.11) | 12 | 120 | 1,440 |

- **客戶編號**：務必填寫，避免像「同名不同分店」被誤判成同一客戶（例如總公司與各分店）。
- **產品名稱(含編號)**：格式為「產品名稱(產品編號)」，系統會自動拆解成產品名稱與產品編號兩個欄位。
- 若客戶編號或產品編號在系統裡不存在，會自動建立新客戶／新產品。
- 同一筆交易（同客戶＋同產品＋同日期＋同金額＋同數量）重複匯入會自動被偵測並略過，不會重複累加業績。
- 點選「Excel 批次匯入」頁面右上角「下載匯入範本」可取得標準格式檔案。

如果你的 Excel 是從 ERP 系統直接匯出的「業務產品別銷貨明細表」原始格式（欄位分散、產品名稱是區塊標題而非每列都有），**先把檔案傳給 Claude**，Claude 會幫你轉換成上面的標準格式再讓你匯入。

## 五、成長 / 下滑 / 停購 的判定邏輯

系統會拿「本月」與「上月」比較（客戶層級是跨產品加總比較，產品層級是該客戶+該產品比較）：

| 狀態 | 判定條件 |
|---|---|
| 成長 | 本月金額比上月成長 5% 以上 |
| 下滑 | 本月金額比上月減少 5% 以上 |
| 持平 | 增減在 ±5% 以內 |
| 停購 | 上月有交易，本月完全沒有 |
| 新客戶／新品 | 首次出現交易紀錄 |

若想調整 5% 門檻，可請 Claude 幫你修改 `app.js` 最上方：
```js
const GROWTH_THRESHOLD = 0.05;
const DECLINE_THRESHOLD = -0.05;
```

## 六、每月建議操作流程

1. 從 ERP／POS 系統匯出當月銷貨交易明細（或請 Claude 幫忙從原始報表轉換格式）。
2. 到「Excel 批次匯入」上傳檔案，確認預覽內容（特別留意「新客戶」「新產品」是否正確）後按「確認匯入」。
3. 到「首頁 Dashboard」查看整體成長/下滑/停購客戶名單。
4. 到「客戶分析」逐一檢視重點客戶的產品別表現，找出哪些產品帶動成長、哪些產品正在流失。
5. 匯出備份檔存到雲端。

## 七、合併帳號（同一客戶有多個編號時）

當客戶編號是「基礎編號-1」「基礎編號-2」這種格式時（例如 B014 與 B014-1），系統會**自動**把 -1 的銷貨合併計算到基礎編號（B014）身上，**完全依編號判斷，不比對客戶名稱**。也就是說，就算兩個編號對應的客戶名稱看起來不一樣，只要編號符合這個規則，一樣會自動合併。

- 到「客戶管理」點右上角「查看合併分組」，可以看到目前系統自動合併了哪些客戶編號（唯讀，不需要手動操作）。
- 若有其他不符合「-1」編號規則、但你仍想手動合併的客戶，可以到編輯客戶畫面的「合併計算於」欄位手動指定。
- 合併後：Dashboard「客戶脈動」與「客戶分析」都只會顯示一行，客戶編號旁邊會有「+N」徽章（滑鼠移上去可看到合併了哪些編號）。

## 八、客戶脈動的篩選與排序

首頁 Dashboard 的「客戶脈動」表格上方新增了：
- **狀態篩選**：只看成長／下滑／停購／持平／新客戶其中一種。
- **排序方式**：預設依異常狀態排序（下滑、停購優先），也可以改成「客戶編號 A→Z」或「客戶名稱筆畫排序」。

## 九、之後想擴充功能怎麼辦？

程式碼都在 `index.html` / `style.css` / `app.js` 三個檔案裡，你不需要自己修改。想加新功能時，把這三個檔案連同你的需求一起貼給 Claude 即可。
